<UserControl
    x:Class="OmegaAssetStudio.WinUI.Modules.MaterialEditor.MaterialGraphEditor.MaterialGraphEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:OmegaAssetStudio.WinUI.Modules.MaterialEditor.MaterialGraphEditor"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
        <Grid Padding="16" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Spacing="6">
                <TextBlock Text="Material Graph Editor" FontSize="22" FontWeight="SemiBold" />
                <TextBlock Text="{Binding StatusText}" Opacity="0.75" TextWrapping="WrapWholeWords" />
                <TextBlock Text="{Binding NodeCountText}" Opacity="0.75" TextWrapping="WrapWholeWords" />
                <TextBlock Text="{Binding ConnectionCountText}" Opacity="0.75" TextWrapping="WrapWholeWords" />
                <TextBlock Text="{Binding SelectedNodeSummaryText}" Opacity="0.75" TextWrapping="WrapWholeWords" />
                <TextBlock Text="{Binding SelectedNodeOutputPreviewText}" Opacity="0.75" TextWrapping="WrapWholeWords" />
            </StackPanel>

            <Grid Grid.Row="1" RowDefinitions="Auto,Auto" RowSpacing="8">
                <Grid ColumnDefinitions="*,*,*" ColumnSpacing="8">
                    <Button Grid.Column="0" Click="ExportLayout_Click">
                        <TextBlock Text="Export Layout" TextWrapping="WrapWholeWords" TextAlignment="Center" />
                    </Button>
                    <Button Grid.Column="1" Click="ImportLayout_Click">
                        <TextBlock Text="Import Layout" TextWrapping="WrapWholeWords" TextAlignment="Center" />
                    </Button>
                    <Button Grid.Column="2" Click="ApplyNodeEdits_Click">
                        <TextBlock Text="Apply Node Edits" TextWrapping="WrapWholeWords" TextAlignment="Center" />
                    </Button>
                </Grid>
                <Grid Grid.Row="1" ColumnDefinitions="*,*" ColumnSpacing="8">
                    <TextBox Grid.Column="0"
                             Header="Node Label"
                             Text="{Binding SelectedNodeLabelText, Mode=TwoWay}" />
                    <TextBox Grid.Column="1"
                             Header="Node Value"
                             Text="{Binding SelectedNodeValueText, Mode=TwoWay}"
                             IsEnabled="{Binding CanEditSelectedNodeParameter}" />
                </Grid>
            </Grid>

            <local:MaterialGraphCanvasControl Grid.Row="2" />
        </Grid>
    </ScrollViewer>
</UserControl>
